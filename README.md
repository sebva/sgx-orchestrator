Project to have scheduling and live-migration for SGX-enabled Docker container running in Swarm mode.

# How to deploy the stack

```
$ docker stack deploy --compose-file=docker-compose.yml monitoring
```
